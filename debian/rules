#!/usr/bin/make -f

export PYBUILD_NAME=yttranscript

%:
	dh $@ --with python3 --buildsystem=pybuild

# Tests are run by upstream CI; skip them during package build to avoid
# pulling pytest/pytest-cov into Build-Depends.
override_dh_auto_test:
	@echo "Skipping tests (handled by upstream CI)"
